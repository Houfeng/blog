- var language = config.language || 'en'

doctype
html(lang=language)
  head
    include head
  body
    div(style="display:none;")
      img(src="/favicons/300x300.png")
    #main
      header
        include header
      section#container
        block container
        block tags
        block pagination
        block comment
      footer
        include footer
    include script
